repos:
  - repo: local
    hooks:
      - id: black
        name: black
        entry: poetry run black
        language: system
        types_or: [python, pyi]

      - id: isort
        name: isort
        entry: poetry run isort
        language: system
        types: [python]
